fastapi==0.100.0          # No change, works with your setup
uvicorn==0.23.1           # No change, latest stable version
selenium==4.14.0          # Update for bug fixes and improvements
webdriver-manager==4.0.0  # Compatible, no change needed
pydantic==1.10.9          # Downgrade due to FastAPI's limited support for Pydantic v2
requests==2.31.0          # No change, latest stable version
